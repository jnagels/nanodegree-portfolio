<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

    </style>

    <style name="ProjectButton" parent="@android:style/Widget.Button">
        <item name="colorButtonNormal">@color/project_button_background_normal</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="ProjectButton.Final">
        <item name="colorButtonNormal">@color/project_button_background_final</item>
    </style>

</resources>
